# 文档管理功能测试指南

## 快速测试步骤

### 准备工作
1. 启动后端服务
   ```bash
   cd backend
   python app.py
   ```

2. 访问知识库管理页面
   ```
   http://localhost:8000/knowledge
   ```

3. 确保至少有一个知识库存在
   - 如果没有，先点击"创建知识库"按钮创建一个

---

## 测试用例

### ✅ 测试 1: 打开文档管理弹窗

**步骤：**
1. 在知识库列表中找到任一知识库卡片
2. 点击卡片底部的"管理文档"按钮

**预期结果：**
- 弹出文档管理 Modal
- 标题显示：文档管理 - [知识库名称]
- 如果知识库为空，显示"暂无文档"提示
- 如果有文档，显示文档列表

**检查点：**
- [ ] 弹窗居中显示
- [ ] 背景半透明遮罩
- [ ] 标题正确显示知识库名称
- [ ] 关闭按钮可见

---

### ✅ 测试 2: 上传单个文档

**步骤：**
1. 在文档管理弹窗中，点击"上传文档"按钮
2. 选择一个 PDF/Word/Excel 文件
3. 等待上传完成

**预期结果：**
- 出现进度条
- 显示"上传中... (1/1) [文件名]"
- 上传完成后显示"上传完成！"
- 2 秒后进度条消失
- 文档列表自动刷新，新文档出现在列表中

**检查点：**
- [ ] 进度条动画流畅
- [ ] 文件名正确显示
- [ ] 上传成功后列表更新
- [ ] 文档图标正确（PDF📄、Word📝等）
- [ ] 上传时间显示正确

**测试文件类型：**
- [ ] .pdf
- [ ] .docx / .doc
- [ ] .xlsx / .xls
- [ ] .pptx / .ppt
- [ ] .txt
- [ ] .md

---

### ✅ 测试 3: 批量上传文档

**步骤：**
1. 点击"上传文档"按钮
2. 同时选择 3-5 个文件（Ctrl+点击多选）
3. 观察上传过程

**预期结果：**
- 进度条逐步前进
- 显示当前上传的文件：(1/5)、(2/5)、... (5/5)
- 全部上传完成后显示"上传完成！"
- 所有文档都出现在列表中

**检查点：**
- [ ] 进度条准确反映上传进度
- [ ] 每个文件名依次显示
- [ ] 不会因为某个文件失败而中断
- [ ] 最终文档数量正确

---

### ✅ 测试 4: 选择文档

**步骤：**
1. 在文档列表中勾选 1 个文档的复选框
2. 观察批量删除按钮变化
3. 再勾选 2 个文档
4. 取消勾选 1 个文档

**预期结果：**
- 勾选后，文档项背景变为浅蓝色
- 批量删除按钮文字变为"批量删除 (1)"
- 再勾选后变为"批量删除 (3)"
- 取消勾选后变为"批量删除 (2)"
- 全部取消后，按钮禁用，显示"批量删除"

**检查点：**
- [ ] 复选框状态正确
- [ ] 选中项高亮显示
- [ ] 批量删除按钮数量实时更新
- [ ] 未选中时按钮禁用
- [ ] 选中时按钮启用

---

### ✅ 测试 5: 批量删除文档

**步骤：**
1. 勾选 2-3 个文档
2. 点击"批量删除 (N)"按钮
3. 在确认对话框中查看文件列表
4. 点击"确定"

**预期结果：**
- 弹出原生确认对话框
- 对话框显示：
  - 知识库名称
  - 删除的文件数量
  - 每个文件的名称（带 • 列表符号）
  - "此操作不可恢复"警告
- 确认后，按钮文字变为"删除中..."
- 删除完成后：
  - 按钮文字短暂显示"删除成功！"
  - 文档列表刷新，删除的文档消失
  - 选中状态清空

**检查点：**
- [ ] 确认对话框内容完整
- [ ] 可以取消删除
- [ ] 删除过程有进度反馈
- [ ] 删除成功后列表更新
- [ ] 复选框状态重置

---

### ✅ 测试 6: 关闭弹窗

**步骤：**
1. 点击右上角关闭按钮
2. 重新打开弹窗，点击弹窗外的背景
3. 重新打开弹窗，按 ESC 键（如果支持）

**预期结果：**
- 弹窗关闭
- 返回知识库列表页面
- 知识库卡片上的文档数量更新（如果有文档变动）

**检查点：**
- [ ] 关闭按钮可用
- [ ] 点击背景可关闭
- [ ] 知识库列表自动刷新
- [ ] 文档数量正确更新

---

### ✅ 测试 7: 空状态显示

**步骤：**
1. 创建一个新的空知识库
2. 打开该知识库的文档管理

**预期结果：**
- 显示空状态图标和提示文字
- "暂无文档"
- "点击「上传文档」按钮添加"
- 批量删除按钮禁用

**检查点：**
- [ ] 空状态居中显示
- [ ] 图标和文字清晰可读
- [ ] 提示用户上传文档

---

### ✅ 测试 8: 响应式布局

**步骤：**
1. 打开浏览器开发者工具 (F12)
2. 切换到移动设备模拟模式
3. 选择不同的设备尺寸：
   - iPhone SE (375px)
   - iPad (768px)
   - Desktop (1920px)

**预期结果：**
- 弹窗宽度自适应
- 按钮和文字大小适配
- 文档列表项布局合理
- 不会出现横向滚动条

**检查点：**
- [ ] 手机端 (< 768px) 弹窗占 95% 宽度
- [ ] 平板端弹窗居中
- [ ] 桌面端弹窗固定 680px
- [ ] 所有文字可读
- [ ] 按钮可点击

---

## 边界情况测试

### 🔍 测试 9: 上传失败处理

**步骤：**
1. 在上传过程中断开网络
2. 或上传超大文件（如果有限制）
3. 或上传不支持的文件类型

**预期结果：**
- 进度条变红
- 显示错误消息：
  - "上传失败: [具体原因]"
- 3 秒后进度条自动隐藏
- 已上传的文档保留，未上传的文档不出现

**检查点：**
- [ ] 错误消息清晰
- [ ] 不会导致页面崩溃
- [ ] 可以重新尝试上传

---

### 🔍 测试 10: 删除失败处理

**步骤：**
1. 选择多个文档
2. 在删除过程中断开网络
3. 或尝试删除不存在的文档

**预期结果：**
- 弹出 alert 提示：
  - "批量删除失败: [原因]"
- 按钮状态恢复为可点击
- 文档列表不变

**检查点：**
- [ ] 错误提示明确
- [ ] 按钮不会卡在"删除中..."状态
- [ ] 可以重新尝试

---

### 🔍 测试 11: 大量文档性能

**步骤：**
1. 上传 50+ 个文档
2. 打开文档管理
3. 滚动文档列表
4. 勾选/取消勾选多个文档
5. 批量删除大量文档

**预期结果：**
- 列表渲染流畅，无明显卡顿
- 滚动平滑
- 复选框响应及时
- 批量删除不超时

**检查点：**
- [ ] 50 个文档时无性能问题
- [ ] 100 个文档时稍有延迟但可接受
- [ ] 200+ 个文档可能需要优化（虚拟滚动）

---

### 🔍 测试 12: 特殊文件名处理

**步骤：**
上传文件名包含特殊字符的文件：
- 中文文件名：`测试文档.pdf`
- 空格：`my document.docx`
- 特殊符号：`report-2024_final(v2).xlsx`

**预期结果：**
- 所有文件正常上传
- 文件名正确显示（无乱码）
- 可以正常删除

**检查点：**
- [ ] 中文文件名正确显示
- [ ] 空格和特殊符号正确处理
- [ ] URL 编码正确（encodeURIComponent）

---

## 浏览器兼容性测试

### 测试浏览器
- [ ] Google Chrome (最新版)
- [ ] Microsoft Edge (最新版)
- [ ] Mozilla Firefox (最新版)
- [ ] Safari (macOS/iOS)

### 测试功能
每个浏览器测试：
- [ ] 打开/关闭弹窗
- [ ] 上传文档
- [ ] 删除文档
- [ ] 复选框交互
- [ ] 进度条显示
- [ ] 样式正确渲染

---

## 自动化测试建议

### 单元测试
```javascript
// 测试 toggleDocSelection 函数
test('toggleDocSelection adds and removes documents', () => {
    state.selectedDocs.clear();
    toggleDocSelection('test.pdf');
    expect(state.selectedDocs.has('test.pdf')).toBe(true);
    
    toggleDocSelection('test.pdf');
    expect(state.selectedDocs.has('test.pdf')).toBe(false);
});

// 测试 getFileIcon 函数
test('getFileIcon returns correct emoji', () => {
    expect(getFileIcon('pdf')).toBe('📄');
    expect(getFileIcon('docx')).toBe('📝');
    expect(getFileIcon('xlsx')).toBe('📊');
});
```

### 集成测试
```javascript
// 测试文档上传流程
test('upload documents updates list', async () => {
    await openDocsModal('test_kb_id');
    const initialCount = state.documents.length;
    
    await handleDocsUpload(mockFileEvent);
    
    expect(state.documents.length).toBeGreaterThan(initialCount);
});
```

---

## 性能基准

### 目标指标
- 弹窗打开时间: < 500ms
- 文档列表渲染 (50 docs): < 300ms
- 单文件上传: < 2s (取决于文件大小和网络)
- 批量删除 (10 docs): < 3s
- 滚动 FPS: > 30fps

### 测量方法
1. Chrome DevTools → Performance
2. 录制操作过程
3. 分析时间线
4. 查找性能瓶颈

---

## 问题报告模板

发现 Bug 时，请按以下格式报告：

```
【Bug 标题】
简短描述问题

【复现步骤】
1. 打开文档管理
2. 点击上传
3. ...

【预期行为】
应该...

【实际行为】
但是...

【环境信息】
- 浏览器: Chrome 120
- 操作系统: Windows 11
- 屏幕尺寸: 1920x1080

【截图/录屏】
（如果有）

【控制台错误】
（打开 F12 查看）
```

---

## 测试完成清单

### 基本功能 ✅
- [ ] 打开弹窗
- [ ] 关闭弹窗
- [ ] 加载文档列表
- [ ] 显示空状态
- [ ] 上传单个文档
- [ ] 批量上传文档
- [ ] 选择文档
- [ ] 批量删除文档

### 错误处理 ✅
- [ ] 上传失败提示
- [ ] 删除失败提示
- [ ] 网络错误处理
- [ ] 文件类型验证

### 用户体验 ✅
- [ ] 加载提示
- [ ] 进度反馈
- [ ] 操作确认
- [ ] 成功提示
- [ ] 列表自动刷新

### 响应式设计 ✅
- [ ] 桌面端布局
- [ ] 平板端布局
- [ ] 手机端布局

### 浏览器兼容性 ✅
- [ ] Chrome
- [ ] Edge
- [ ] Firefox
- [ ] Safari

---

## 测试报告示例

```
测试日期: 2024-08-11
测试人员: [姓名]

测试结果统计:
- 通过: 45 / 50
- 失败: 3 / 50
- 阻塞: 2 / 50

主要问题:
1. [P1] 上传超过 50 个文档后滚动卡顿
2. [P2] Safari 浏览器进度条显示异常
3. [P3] 长文件名溢出

建议:
1. 添加虚拟滚动优化大量文档渲染
2. 修复 Safari 兼容性问题
3. 文件名添加 text-overflow 处理
```

---

**测试愉快！如有问题请随时反馈。** 🎉
