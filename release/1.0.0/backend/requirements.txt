# ============================================================
# 智能客服系统 - 后端依赖
# Python >= 3.11
# ============================================================

# Web 框架
fastapi==0.138.1
uvicorn==0.49.0
python-multipart==0.0.32      # FastAPI 文件上传支持

# LangChain 生态
langchain==1.3.8
langchain-core==1.4.6
langchain-ollama==1.1.0        # Ollama LLM / Embeddings
langchain-text-splitters==1.1.2
# 注意：langchain-community 已标记废弃，仅用于 Docx2txtLoader/PyPDFLoader
# 后续可迁移至 langchain-docling 或 unstructured
langchain-community==0.4.2

# 向量数据库
chromadb==1.5.9

# 文档解析
pypdf==6.13.2                  # PDF 解析
# docx/doc 解析由 langchain-community 内置依赖提供（docx2txt）
# .txt / .md 直接使用 TextLoader，无额外依赖

# Token 计数（用于 RecursiveCharacterTextSplitter.from_tiktoken_encoder）
tiktoken==0.13.0

# HTTP 客户端（用于 Ollama API 调用，需绕过系统代理）
httpx==0.28.1

# 数据校验
pydantic==2.13.4

# 配置管理
python-dotenv==1.2.1
